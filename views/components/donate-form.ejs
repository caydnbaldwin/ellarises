<div class="col-md-7 pe-md-4">
  <h2 class="mb-2 site-title">Rise with Ella <span class="">— Empowering Futures</span></h2>
  <p class="hero-sub">We believe in the power of community and the brilliance of our youth. Your contribution helps build mentorship, scholarships, and life-changing opportunities.</p>
  <p class="lead-accent">Every gift matters — join our mission.</p>
  <div class="mt-3">
    <div class="donation-amount">Suggested: <strong>$25 • $50 • $100</strong></div>
  </div>
</div>
<div class="col-md-5 mt-4 mt-md-0">
  <h4>Already have an account? <a href="/persons/login" style="color:var(--c2); font-weight:600; text-decoration:none;">Login</a></h4><br>
  <form action="/donations/donate" method="POST" class="needs-validation" novalidate>
    <div class="mb-3">
      <label for="email" class="form-label">Email</label>
      <input type="email" id="email" name="email" class="form-control" placeholder="example@email.com" required>
      <div class="invalid-feedback">Please enter a valid email.</div>
    </div>

    <div class="mb-3">
      <label for="password" class="form-label">Password</label>
      <input type="password" id="password" name="password" class="form-control" placeholder="Create a password" required>
      <div class="tiny-muted">You don't need to already have an account to donate.</div>
    </div>

    <div class="mb-3">
      <label for="donationamount" class="form-label">Donation Amount (USD)</label>
      <input type="number" step="0.01" id="donationamount" name="donationamount" class="form-control" placeholder="20.00" required>
      <div class="invalid-feedback">Enter an amount greater than 0.</div>
    </div>

    <div class="d-flex gap-2">
      <button type="submit" class="btn btn-primary-custom flex-fill" onsubmit="return confirm('Thank you so much for supporting us!');">Donate</button>
      <a href="/" class="btn btn-outline-custom">Cancel</a>
    </div>
  </form>
</div>
