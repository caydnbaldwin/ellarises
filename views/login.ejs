<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
  <link rel="icon" type="image/x-icon" href="/images/bromunity/favicon.ico">
</head>

<body class="bg-light">
  <div class="container d-flex flex column align-items-center justify-content-center min-vh-100">
    <div class="text-center mb-5">
      <!-- <img src="/images/bromunity/BromunityLogo.svg" class="rounded mx-auto d-block" style="max-height: 300px; max-width: 300px;" alt="Bromunity logo"> -->
      <h1 class="display-4 text-dark mb-3">Ella Rises</h1>
      <p class="text-muted">Log in to invite young women to pursue higher education and embrace their heritage by providing culturally rooted programs and educational experiences.</p>
      <div class="mx-auto w-50">
        <div class="bg-white p-4 rounded shadow">
          <h3 class="text-center mb-4">Login</h3>
          <% if (errorMessage && errorMessage !== '') { %>
            <div class="alert alert-danger text-center" role="alert" id="errorMessage">
              <%= errorMessage %>
            </div>
          <% } %>
          <form action="/persons/login" method="POST">
            <div class="text-start mb-3">
              <label for="username" class="form-label">Username</label>
              <input type="text" id="username" name="username" class="form-control" placeholder="example@email.com" required autofocus>
            </div>
            <div class="text-start mb-3">
              <label for="password" class="form-label">Password</label>
              <input type="password" id="password" name="password" class="form-control" required>
            </div>
            <div class="d-grid">
              <button type="submit" class="btn btn-dark">Login</button>
            </div>
          </form>
        </div>
      </div>
      <p class="text-muted py-3">Don't have an account? <a href="/persons/signup" class="text-dark">Sign Up</a></p>
    </div>
  </div>
</body>

</html>