<!DOCTYPE html>
<html lang="en">
<head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Add Survey</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
        <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display&family=Montserrat:wght@300;400;600;700&display=swap" rel="stylesheet">
        <style>
            :root{ --c2:#978EC4; }
            body{font-family:'Montserrat',system-ui,-apple-system,'Segoe UI',Roboto,'Helvetica Neue',Arial}
            .page-title{font-family:'DM Serif Display',serif; color:var(--c2)}
        </style>
</head>
<body class="bg-light">
    <!-- Brings in the navbar from the components directory -->
    <%- include('components/navbar') %>
        <main class="container px-3 px-md-4 py-4">
            <div class="row">
                <div class="col-12 col-lg-8 col-xl-6 mx-auto">
                    <div class="d-flex justify-content-between align-items-baseline mb-3">
                        <h1 class="h3 m-0 page-title">Complete Survey</h1>
                        <span class="text-secondary">Event: <%= survey.eventname %></span>
                    </div>

                    <div class="card shadow-sm">
                        <div class="card-body">
                            <form action="/survey/add" method="post">
                                <div class="mb-3">
                                    <label for="surveysatisfactionscore" class="form-label">Satisfaction Score</label>
                                    <input type="number" id="surveysatisfactionscore" name="surveysatisfactionscore" class="form-control">
                                </div>

                                <div class="mb-3">
                                    <label for="surveyusefulnessscore" class="form-label">Usefulness Score</label>
                                    <input type="number" id="surveyusefulnessscore" name="surveyusefulnessscore" required class="form-control">
                                </div>

                                <div class="mb-3">
                                    <label for="surveyinstructorscore" class="form-label">Instructor Score</label>
                                    <input type="number" id="surveyinstructorscore" name="surveyinstructorscore" required class="form-control">
                                </div>

                                <div class="mb-3">
                                    <label for="surveyrecommendationscore" class="form-label">Recommendation Score</label>
                                    <input type="number" id="surveyrecommendationscore" name="surveyrecommendationscore" required class="form-control">
                                </div>

                                <div class="d-flex justify-content-end gap-2">
                                    <a href="/surveys/surveys" class="btn btn-outline-secondary">Cancel</a>
                                    <button type="submit" class="btn btn-primary">Submit</button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </main>
</body>
</html>