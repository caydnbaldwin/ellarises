<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Onboarding</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
  <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display&family=Montserrat:wght@300;400;600;700&family=Great+Vibes&display=swap" rel="stylesheet">
  <link rel="icon" type="image/x-icon" href="/images/bromunity/favicon.ico">
  <style>
    :root{
      --c1: #99B7C6;
      --c2: #978EC4;
      --c3: #F9F5EA;
      --c4: #FFD8D1;
      --c5: #F9AFB1;
      --c6: #9AB59D;
      --c7: #3A3F3B;
      --c8: #F4B092;
      --c9: #CE325B;
      --radius: 14px;
    }

    body{font-family: 'Montserrat', system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial; background: linear-gradient(180deg,var(--c3) 0%, rgba(153,183,198,0.06) 100%); color: var(--c7);}
    .site-title{font-family: 'DM Serif Display', serif; font-size: clamp(1.6rem, 3.2vw, 2.6rem); letter-spacing: .6px; color: var(--c2);}
    .auth-card{background: linear-gradient(180deg, rgba(255,216,209,0.9), rgba(249,245,234,0.95)); border-radius: var(--radius); box-shadow: 0 8px 30px rgba(58,63,59,0.12); border: 1px solid rgba(152,142,196,0.08);}
    .form-label{font-weight:600; color:var(--c7)}
    .btn-primary-custom{background: linear-gradient(90deg,var(--c2),var(--c9)); border: none; color: white; font-weight:700; border-radius: 10px; padding: .65rem 1.25rem}
    .tiny-muted{font-size:.9rem; color: rgba(58,63,59,0.65)
    }

    html {
  height: 100%;
  background: linear-gradient(180deg, var(--c3) 0%, rgba(153,183,198,0.06) 100%);
}
  </style>
</head>

<body>
  <!-- Brings in the navbar from the components directory -->
  <%- include('components/navbar') %>
  <div class="container py-5">
    <div class="row justify-content-center align-items-center">
      <div class="col-md-8 col-lg-6">
        <div class="d-flex justify-content-center mb-4">
          <div class="text-center">
            <h1 class="site-title mb-1">Ella Rises</h1>
            <div class="tiny-muted">Fill out your profile information.</div>
          </div>
        </div>
        <div class="auth-card p-4 p-md-5 mx-auto" style="max-width:720px;">
          <h3 class="mb-3">Onboarding</h3>
          <% if (errorMessage && errorMessage !== '') { %>
            <div class="alert alert-danger text-center" role="alert" id="errorMessage">
              <%= errorMessage %>
            </div>
          <% } %>
          <form action="/persons/onboarding" method="POST" class="needs-validation">
            <div class="mb-3">
              <label for="firstName" class="form-label">First Name</label>
              <input type="text" id="firstName" name="firstName" class="form-control" placeholder="Jane" required autofocus>
            </div>
            <div class="mb-3">
              <label for="lastName" class="form-label">Last Name</label>
              <input type="text" id="lastName" name="lastName" class="form-control" placeholder="Smith" required>
            </div>
            <div class="mb-3">
              <label for="dateOfBirth" class="form-label">Date of Birth</label>
              <input type="date" id="dateOfBirth" name="dateOfBirth" class="form-control" required>
            </div>
           
            <div class="mb-3">
              <label for="phone" class="form-label">Phone</label>
              <input 
                type="tel" 
                id="phone" 
                name="phone" 
                class="form-control"
                pattern="\d{3}-\d{3}-\d{4}"
                placeholder="123-456-7890"
                required
              >
              <!-- <input type="text" id="phone" name="phone" class="form-control" placeholder="123-456-7890" required> -->
            </div>
            <div class="mb-3">
              <label for="city" class="form-label">City</label>
              <input type="text" id="city" name="city" class="form-control" placeholder="Provo" required>
            </div>
            <div class="mb-3">
              <label for="state" class="form-label">State</label>
              <select name="state" id="state" class="form-select" required>
                <option value="" disabled selected>Select a state</option>
                <% for (const state of states) { %>
                  <option value="<%= state.state %>"><%= state.state %></option>
                <% } %>
              </select>
            </div>
            <div class="mb-3">
              <label for="zipCode" class="form-label">Zip Code</label>
              <input type="text" id="zipCode" name="zipCode" class="form-control" placeholder="123456" required>
            </div>
            <div class="mb-3">
              <label for="organization" class="form-label">Organization</label>
              <input type="text" id="organization" name="organization" class="form-control" placeholder="Ella Rises" required>
            </div>
            <div class="mb-3">
              <label for="fieldOfInterest" class="form-label">Field of Interest</label>
              <select name="fieldOfInterest" id="fieldOfInterest" class="form-select" required>
                <option value="" disabled selected>Select a field</option>
                <% for (const fieldOfInterest of fieldsOfInterest) { %>
                  <option value="<%= fieldOfInterest.fieldOfInterest %>"><%= fieldOfInterest.fieldOfInterest %></option>
                <% } %>
              </select>
            </div>
            <div class="d-grid">
              <button type="submit" class="btn btn-lg text-white" style="background-color: #F9AFB1; border-color: #F9AFB1;">Submit</button>
              <br>
              <a class="btn btn-primary" style="background-color: #9AB59D; border-color: #9AB59D;" href="/">Skip</a>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</body>

</html>